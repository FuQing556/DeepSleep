# HS睡姿呆毛局部清理

原文件raw/为现有已确认睡姿备份，与旧候选hash相同，并非重新生成。只在ROI(345,78)-(490,200)中以(420,120)为种子移除连通中性棋盘，保留黑色轮廓，处理一像素混色边。

删除6307背景像素，含窄边共改变6686像素；y>=220的全部身体像素逐字节未改。1254方形画布、可见bbox和Unity导入参数保持不变，结果已导入原PNG路径，GUID保留。

white/dark/sky_gap为原尺寸局部预览；最终机器检查在previews/audit_final。previews/audit是初次选错保护点的检查记录：465,110属于孔内浅棋盘而非发丝，因此当时失败；新保护点从源图取色确认，并未为通过检查再次修改图片。

算法、原hash和输出统计见clean_hair_gap.py与report.json。没有重画人物、改变头饰颜色或全图删除白色。
